@import super();

.secret-title {
  alignment-x: center;
  text-color: green;
  font-size: 1.5;
  top: 0;
}

.secret-container {
  top: 20;
  bottom: 20;
  left: 20;
  right: 20;
  filter: blur(25);
  border-radius: 15;
  border-thickness: 5;
  border-color: var(--background-color-border);
  background-color: var(--background-color);
  box-shadow: none;
}

.chat-reconnect-title {
  top: 60;
  left: 50;
  font-size: 1.4;
}

.chat-reconnect-switch {
  top: 60;
  left: 200;
  height: 14;
  width: 25;
}

.showJoinsTitle {
  top: 100;
  left: 50;
  font-size: 1.4;
}

.showJoinsSwitch {
  top: 100;
  left: 200;
  height: 14;
  width: 25;
}