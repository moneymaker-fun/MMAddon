@import super();

.booster-title {
  alignment-x: center;
  text-color: green;
  font-size: 1.5;
}
.secret-container {
  top: 40;
  bottom: 20;
  left: 20;
  right: 20;
  filter: blur(25);
  border-radius: 15;
  border-thickness: 5;
  border-color: var(--background-color-border);
  background-color: var(--background-color);
}
.showJoinsTitle {
  top: 60;
  left: 50;
  font-size: 1.4;
}
.showJoinsSwitch {
  top: 60;
  left: 165;
  height: 14;
  width: 25;
}
.exportOnShutdownTitle {
  top: 80;
  left: 50;
  font-size: 1.4;
}
.exportOnShutdownSwitch {
  top: 80;
  left: 165;
  height: 14;
  width: 25;
}