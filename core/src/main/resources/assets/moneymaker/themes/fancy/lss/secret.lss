@import super();

.booster-title {
  alignment-x: center;
  text-color: green;
  font-size: 1.5;
}
.secret-container {
  top: 40;
  bottom: 20;
  left: 20;
  right: 20;
  filter: blur(25);
  border-radius: 15;
  border-thickness: 5;
  border-color: var(--background-color-border);
  background-color: var(--background-color);
}
.showJoinsTitle {
  top: 60;
  left: 50;
  font-size: 1.5;
}
.showJoinsSwitch {
  top: 60;
  left: 160;
  height: 16;
  width: 32;
}